---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: plex
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - "plex.k8s.alexieff.io"
  rules:
    - backendRefs:
        - name: plex
          port: 32400
