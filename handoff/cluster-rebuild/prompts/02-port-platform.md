# Fresh Session Prompt — Port the Platform

You are working in the fresh target repository generated by an earlier session. You have no prior chat context. This session is offline preparation and must not mutate the live cluster.

## Inputs

- Extracted handoff package path.
- Target repository working tree and remote.
- Required predecessor artifact: `handoff-status/01-target-generated.yaml` with all checks passing.

## Required reading

Read `START-HERE.md`, `mappings/template-overlap.yaml`, `mappings/source-to-target.yaml`, `inventories/dependencies.yaml`, `runbooks/02-port-platform.md`, and the generated implementations for every overlapping component before editing.

## Task

1. Verify the target working tree is based on template commit `bb09348610aa5bee9505230b99c21c08db15bf6d` and the predecessor artifact matches its current baseline.
2. For each record in `template-overlap.yaml`, compare generated and payload implementations completely.
3. Keep the template-generated resource as sole owner and carry only intentional current deviations.
4. Replace the template echo example with the current echo behavior without creating a duplicate app.
5. Port non-template wave 1–4 components in dependency order using their exact target mappings.
6. Correct sourceRef/path/namespace/dependsOn/substitution references to target conventions.
7. Keep secret resources structurally referenced but gated for the secret session.
8. Run `just configure`, generated repository validation, YAML parsing, reference checks, ownership checks, and platform mapping parity after each group.
9. Commit logical groups and create `handoff-status/02-platform-ported.yaml`.

## Boundaries and stop conditions

- Never bulk-copy legacy bootstrap/Talos/Flux roots over generated output.
- Never leave two HelmReleases/Kustomizations/Gateways/chart sources owning one component.
- Never delete a dependency to silence validation.
- Stop on chart/API incompatibility, unresolved references, duplicate ownership, SOPS plaintext, missing CRD owner, or a failed generated-template validation.
- Do not run bootstrap/apply/reconcile commands against a cluster.

## Deliverable

Return target commits, overlap decisions, all completed wave 1–4 IDs/paths, compatibility changes, exact validation results, `handoff-status/02-platform-ported.yaml`, blockers, and `clusterMutationPerformed: false`.
